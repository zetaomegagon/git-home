#!/usr/bin/env bash

input="$@"

emacs-tty() {
    source $HOME/.bashrc.d/12-emacs.rc
    local finput="$@"
    
    emacs-dead-p && emacsctl start

    if [[ -z "$finput" ]]; then
	command emacsclient --tty
    else
	command emacsclient --tty "$finput"
    fi    
}

emacs-tty "$input"
